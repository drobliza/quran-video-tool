name: Test MoviePy

on: [push, pull_request]

jobs:
  test-moviepy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install moviepy

      - name: Test moviepy.editor
        run: |
          python -c "from moviepy.editor import VideoFileClip; print('✅ MoviePy is working')"
